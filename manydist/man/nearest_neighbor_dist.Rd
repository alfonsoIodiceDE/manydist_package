% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knn_dist_functions.R,
%   R/knn_dist_parsnip_registration.R
\name{fit_knn_dist}
\alias{fit_knn_dist}
\alias{predict_knn_dist_class}
\alias{predict_knn_dist_prob}
\alias{predict_knn_dist_reg}
\alias{nearest_neighbor_dist}
\title{Nearest neighbor with precomputed distances}
\usage{
fit_knn_dist(x, y, k = 5, dist_fun = NULL, dist_args = list())

predict_knn_dist_class(object, new_data, type = c("class", "prob"))

predict_knn_dist_prob(object, new_data, ...)

predict_knn_dist_reg(object, new_data)

nearest_neighbor_dist(mode = "classification", neighbors = NULL)
}
\arguments{
\item{x}{A data frame or matrix of predictors. If `dist_fun = NULL`,
`x` is treated as an already computed distance representation.}

\item{y}{A vector of outcomes.}

\item{k}{Number of neighbours.}

\item{dist_fun}{Optional distance function taking arguments `x` and
`new_data`. If `NULL`, inputs are assumed to already be distance matrices.}

\item{dist_args}{A named list of additional arguments passed to `dist_fun`.}

\item{object}{A fitted `knn_dist` object created by `fit_knn_dist()`.}

\item{new_data}{A data frame or matrix of precomputed distances, or raw
predictors when `dist_fun` is supplied.}

\item{type}{Prediction type. For classification, `"class"` returns class
predictions and `"prob"` returns class probabilities.}

\item{...}{Additional arguments passed to lower-level methods or currently
not used.}

\item{mode}{"classification" or "regression".}

\item{neighbors}{Number of neighbors (k).}
}
\description{
Nearest neighbor with precomputed distances
}
\keyword{internal}
